interface:
  display_name: "Reverse Editing Workflow"
  short_description: "Build safe editable reverse-video workflow packages"
  default_prompt: "Use $reverse-editing-workflow to turn this reference into a safe local editable workflow package with video-specific slot count and default-deny external actions."
